#include <stdio.h>

/**
 * main - Entry point
 * Description: function to display a string using printf
 * Return: should return 0;
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
