#include <stdio.h>

/**
 * main - Entry point
 * Description: function to display a string using puts
 * Return: should return 0;
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
